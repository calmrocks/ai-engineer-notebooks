# utils

Tiny helpers notebooks may import (client setup, pretty-printing).
Deliberately minimal — the moment this grows abstractions over the
concepts the notebooks teach, it has failed. Resist building a framework.
